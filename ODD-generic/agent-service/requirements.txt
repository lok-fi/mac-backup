google-adk>=1.0.0
# MCP client SDK — ADK's MCPToolset imports it lazily, so it's not pulled in
# automatically. Needed for /mcp/connect & /mcp/ingest (Streamable-HTTP transport).
mcp>=1.8.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
httpx>=0.27.0
