# Secrets — never commit these
.env
functions/FivaWebhook/catalyst-config.json

# SSL certs (self-signed, local only)
key.pem
cert.pem

# Node
node_modules/

# Zoho pack output
dist/

# Data files with phone numbers (optional — remove if you want to commit them)
server/data/authorized-numbers.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log
ZET-debug.log
